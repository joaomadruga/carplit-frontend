import { useContext } from 'react';
import { Image, SafeAreaView, StyleSheet, Text, View } from 'react-native';

export default function Finance() {
    return (
        <SafeAreaView>
            <Text>Hello finance screen!</Text>
        </SafeAreaView>
    );
}